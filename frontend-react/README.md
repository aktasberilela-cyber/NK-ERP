# Frontend React (Vite) prototype

Bu klasör React + Vite tabanlı basit bir prototip içerir.

Çalıştırma (lokal):
1. frontend-react dizinine girin
2. npm install
3. npm run dev

Daha sonra UI bileşenleri geliştirilecek: Message composer, API bağlantısı, routing vs.
